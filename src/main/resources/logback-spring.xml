<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Spring Boot 기본 설정 -->
  <include resource="org/springframework/boot/logging/logback/base.xml"/>

  <!-- 콘솔 로그 패턴 오버라이드 (MDC 추가) -->
  <property name="CONSOLE_LOG_PATTERN"
    value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr([req:%X{requestId:-}]){cyan} %clr([uid:%X{userId:-}]){yellow} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n"/>

  <property name="LOG_PATH" value="${LOG_PATH:-logs}"/>

  <!-- ===================== -->
  <!-- APP 로거 -->
  <!-- ===================== -->
  <appender name="JSON_FILE"
    class="ch.qos.logback.core.rolling.RollingFileAppender">

    <file>${LOG_PATH}/app.json.log</file>

    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/app.%d{yyyy-MM-dd}.json.log</fileNamePattern>
      <maxHistory>7</maxHistory>
    </rollingPolicy>

    <!-- WARN + ERROR만 수집 -->
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>WARN</level>
    </filter>

    <!-- MDC 자동 포함 -->
    <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
  </appender>

  <!-- ===================== -->
  <!-- EVENT 로거 -->
  <!-- ===================== -->
  <appender name="DOMAIN_EVENT_FILE"
    class="ch.qos.logback.core.rolling.RollingFileAppender">

    <file>${LOG_PATH}/event.json.log</file>

    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/event.%d{yyyy-MM-dd}.json.log</fileNamePattern>
      <maxHistory>7</maxHistory>
    </rollingPolicy>

    <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>

    <filter class="ch.qos.logback.classic.filter.LevelFilter">
      <level>INFO</level>
      <onMatch>ACCEPT</onMatch>
      <onMismatch>DENY</onMismatch>
    </filter>
  </appender>

  <logger name="seondays.shareticon.logging.DomainEventLogListener"
    level="INFO"
    additivity="false">
    <appender-ref ref="DOMAIN_EVENT_FILE"/>
    <appender-ref ref="CONSOLE"/>
  </logger>

  <!-- ===================== -->
  <!-- 루트 로거 -->
  <!-- ===================== -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="JSON_FILE"/>
  </root>

</configuration>
