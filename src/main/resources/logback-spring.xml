<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Spring Boot 기본 설정 -->
  <include resource="org/springframework/boot/logging/logback/base.xml"/>

  <!-- 콘솔 로그 패턴 오버라이드 (MDC 추가) -->
  <property name="CONSOLE_LOG_PATTERN"
    value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr([req:%X{requestId:-}]){cyan} %clr([uid:%X{userId:-}]){yellow} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n"/>

  <!-- ===================== -->
  <!-- JSON 파일 (수집용) -->
  <!-- ===================== -->
  <appender name="JSON_FILE"
    class="ch.qos.logback.core.rolling.RollingFileAppender">

    <file>logs/app.json.log</file>

    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>logs/app.%d{yyyy-MM-dd}.json.log</fileNamePattern>
      <maxHistory>7</maxHistory>
    </rollingPolicy>

    <!-- WARN + ERROR만 수집 -->
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>WARN</level>
    </filter>

    <!-- MDC 자동 포함 -->
    <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
  </appender>

  <!-- ===================== -->
  <!-- 루트 로거 -->
  <!-- ===================== -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="JSON_FILE"/>
  </root>

</configuration>
